---
import { twMerge } from "tailwind-merge";

interface Props {
  styles: string;
}

const { styles } = Astro.props;

const clases = twMerge(
  "px-2 py-1 rounded-md bg-slate-200 dark:bg-slate-600 fixed z-50 shadow-md",
  styles
);
---

<div class=`${clases}` id="smallModal">
  <slot />
</div>
